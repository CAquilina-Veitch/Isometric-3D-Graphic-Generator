import styles from './Overlays.module.css';

export default function Overlays() {
  return (
    <div className={styles.overlays}>
      {/* Toasts, shortcut help modal, banners mount here (M17) */}
    </div>
  );
}
